import React from 'react';
import { render } from 'react-dom';

import './style.scss';

const App = () => (
  <div>
    <h1>Boeeenas</h1>
  </div>
);

render(<App />, document.getElementById('app'));
